# Core dependencies — installable with `pip install -r requirements.txt`.
# For development, prefer `pip install -e .[nllb,dev]` from the repo root.

deep-translator>=1.11
matplotlib>=3.7
numpy>=1.24
pandas>=2.0
sacrebleu>=2.4
scikit-learn>=1.3
scipy>=1.11
seaborn>=0.13
sentence-transformers>=2.6
spacy>=3.7

# After install you also need to download the spaCy model:
#   python -m spacy download en_core_web_md

# Optional — required only for the NLLB-200 backend:
# torch>=2.1
# transformers>=4.40
# sentencepiece>=0.2
